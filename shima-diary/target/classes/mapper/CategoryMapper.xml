<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shima.diary.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.shima.diary.entity.Category">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="name" property="name" />
        <result column="icon" property="icon" />
        <result column="color" property="color" />
        <result column="sort_order" property="sortOrder" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, name, icon, color, sort_order, create_time, update_time, deleted
        FROM category
        WHERE deleted = 0 AND user_id = #{userId}
        ORDER BY sort_order ASC, create_time DESC
    </select>

    <select id="selectByUserIdAndName" resultMap="BaseResultMap">
        SELECT id, user_id, name, icon, color, sort_order, create_time, update_time, deleted
        FROM category
        WHERE deleted = 0 AND user_id = #{userId} AND name = #{name}
    </select>

</mapper>